"""Penn & Turner space-syntax agent simulator.

Standalone tool: reads a Web-Based JuPedSim scenario (JSON + WKT), runs the
EVA agent model (Turner & Penn 2002), writes a JuPedSim-compatible sqlite
trajectory the web app can visualise unchanged.
"""

__version__ = "0.1.0"
